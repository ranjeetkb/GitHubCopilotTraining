using Microsoft.AspNetCore.Http;
using Microsoft.AspNetCore.Mvc;

namespace BookManagemntApp.Controllers
{
    
    // Controller Pattern (MVC) + Dependency Injection + Factory
    // Create a BookController that injects IBookService and exposes CRUD endpoints using REST
   
}
